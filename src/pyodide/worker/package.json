{
  "name": "pyodide-worker",
  "version": "1.0.0",
  "description": "Bundled Pyodide Worker",
  "main": "worker.js",
  "scripts": {
    "build": "webpack --config webpack.config.cjs --mode=production",
    "build-dev": "webpack --config webpack.config.cjs --mode=development",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["pyodide", "worker", "python"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "babel-loader": "^9.1.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.0"
  }
}
